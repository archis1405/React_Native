The ReactNative Repository 

// Command to setup a Reaact_Native project
    -- npx create create-expo-app@latest

// Command to run the application
    -- npm start

    -- then scaan the QR in ExpoGo application 